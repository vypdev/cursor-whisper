# Use hoisted node_modules for better VSCode extension compatibility
node-linker=hoisted

# Auto-install peer dependencies
auto-install-peers=true

# Keep semver ranges in package.json
save-exact=false
